# CON-001 Evidence - 10-Tool Contract from Pydantic Models

- Work ID: `CON-001`
- Canonical Issue: #32
- Dependency: G2 PASS + PKG-001
- Status: **Complete**
- Implementation baseline: `develop@a84a0b4`
- Implementation PR: #97
- Next serial Work ID: `MEM-001` / #33

## 1. What was done

Three strict XFAILs converted to passing tests:

| XFAIL ID | Problem | Fix |
|---|---|---|
| XF-CONTRACT-001 | memory_filter sent `lambda_value`, API expects `lambda` alias | `_tool_memory_filter` now sends `{"lambda": ...}` |
| XF-CONTRACT-002 | checkpoint_create schema missing `plan_steps`, `completed_step_ids` in required | Added to required array |
| XF-CONTRACT-003 | plan_update_step schema described `blocked`, not in PlanStatus enum | Replaced with `enum: [pending, in_progress, completed, pending_review]` |

`memory_store` schema added to `_TOOL_SCHEMAS` (10th target tool). Handler pending MEM-001.
`_validate_runtime_registry()` updated: schemas = target 10, handlers = runtime 9.

## 2. CI evidence

PR #97:
- test (3.10): SUCCESS
- test (3.11): SUCCESS
- test (3.12): SUCCESS

## 3. Test coverage

TC-CONTRACT-001–010: 20 test cases in `tests/test_tool_contract.py`.
Local: 107 passed, 2 xfailed (AUD-001, MEM-002 — not in scope).

## 4. XFAIL inventory after CON-001

| XFAIL ID | Owner | Status |
|---|---|---|
| XF-AUDIT-001 | AUD-001 | Still xfail |
| XF-MEM-001 | MEM-002 | Still xfail |

2 strict XFAIL remaining (was 5 before CON-001).
