# REL-001 Evidence：版本语义统一

- Work ID：`REL-001`
- Issue：#5
- 分支：`chore/rel-001-version-contract`
- 状态：待 PR/CI 完成后关闭

## 1. 可证伪假设

根 distribution、Harness Plugin 和 Context Plugin 属于同一协调发布身份，但基线分别为 `2.0.0`、`2.2.0`、`0.1.0`。将 PEP 440 `3.0.0b1` 映射为 Plugin SemVer `3.0.0-beta.1`，并把 Python 支持限制为 `>=3.10,<3.13`，可以消除版本冲突而不改变 Runtime 行为。

## 2. 修改结果

- `pyproject.toml`：`3.0.0b1`，Python `>=3.10,<3.13`。
- 两个 Plugin manifest：`3.0.0-beta.1`。
- `test_project_versions_are_consistent`：删除 strict XFAIL，验证 PEP 440 ↔ SemVer 映射和 Python 范围。
- `docs/decisions/VERSIONING.md`：记录版本、Tag、Beta 递增、Stable、Changelog 和 Migration 策略。
- `CHANGELOG.md`：增加 Unreleased Beta 稳定化条目，历史版本不改写。

## 3. 边界

本任务不发布 Tag/Release/package，不添加依赖，不修改 package layout、installer、Tool 或业务行为。`XF-DEPS-001` 仍由 `PKG-002` 负责。

## 4. 预期验证

- 版本目标测试普通 Pass。
- Python 3.10、3.11、3.12 完整 CI 通过。
- 已知 strict XFAIL 数量由 12 降为 11；无 XPASS、Failed 或 Error。
- G0 仍未通过。
