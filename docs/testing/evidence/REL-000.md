# REL-000 Evidence：规格发布契约同步

- Work ID：`REL-000`
- Issue：#3
- Pull Request：#4
- 分支：`docs/rel-000-contract-sync`
- 结论：规格文件已同步；不修改业务代码、package metadata、manifest、workflow、installer 或历史测试报告。

## 1. 修改前假设与判断

假设：PRD、产品架构、技术架构、测试计划和文档索引存在旧版号、9/10 Tool、CLI/卸载、安全默认值和 Tag 顺序冲突。核对后假设成立，已以计划 v2.2 + Errata 2.3 为唯一目标契约进行执行基线规范化，并保留全部 88 个功能需求编号。

## 2. 固定契约

- Git/GitHub `v3.0.0-beta.1`；Python `3.0.0b1`；Stable `v3.0.0`。
- 当前 9 Tool；Beta 目标 10 Tool；`memory_store` 由 `CON-001 + MEM-001` 实现。
- pip 管理 distribution；CLI 管理部署、配置、数据、Server 和诊断。
- Linux/macOS；Python 3.10–3.12；单一真实 Hermes 正式版本。
- loopback；Summary false；Memory startup import false；log content false。
- G3 使用冻结 Commit/RC Tag；正式 Beta Tag 由 `BETA-001` 创建。
- 规范 XFAIL：`XF-DEPS-001`、`XF-RELEASE-001`。

## 3. 机器化计数

- PRD `FR-*`：88 个唯一 ID。
- Test Plan `TC-*`：100 个唯一 ID。
- 既有 Test ID：83；新增预留：17；合计：100。
- 每个 Test ID 在表中有一个主责 Work ID。

## 4. 冲突搜索

以下活动规格文件中均为 0：

- `v0.3.0-beta`
- `v0.4.0-beta`
- `v1.0.0` 作为本周期目标
- `HARNESS_SERVER_PORT`
- `HARNESS_SERVER_URL`
- 9 个 Tool 被描述为最终 Beta Contract
- 自动安装 Cron
- 普通 uninstall 删除 Python distribution

历史测试报告和主计划 v2.2 不回写；主计划冲突由 Errata 2.3 规范性覆盖，并由 `REL-002` 合并。

## 5. 三层证据

- 文件层：5 个规格文件可解析，88 FR 和 100 TC 唯一。
- 集成层：PRD、两份架构、Test Plan 和 Index 使用相同版本、Tool、CLI、安全和 Tag 契约。
- 管线层：版本/安装 → Runtime probes → 10 Tool target → 数据完整性 → test-release → RC/正式 Tag 顺序形成闭环。

## 6. 改动边界

PR #4 只包含以下授权路径：

- `docs/product/PRD.md`
- `docs/architecture/PRODUCT_ARCHITECTURE.md`
- `docs/architecture/TECHNICAL_ARCHITECTURE.md`
- `docs/testing/TEST_PLAN.md`
- `docs/README.md`
- `docs/testing/evidence/REL-000.md`
- `docs/traceability/RELEASE_TRACEABILITY.md`

## 7. 未解决风险

- 本 Work ID 只同步规格，不实现功能。
- `REL-001` 负责实际版本和 `requires-python`。
- `REL-002` 负责把 Errata 合并回主计划和分支 Ruleset。
- `REL-004` 只冻结目标 Tool 清单，不实现 `memory_store`。
- G0 仍未通过。
