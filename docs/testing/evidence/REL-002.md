# REL-002 Evidence：分支工作流与仓库治理

- Work ID：`REL-002`
- Issue：#7
- Part A Pull Request：#8
- Part A Merge Commit：`e7e1414e2a20fc1533d6bc6ff285dbd989fb0fe6`
- Part C 分支：`docs/rel-002-plan-consolidation`
- 状态：等待 Plan Consolidation PR 的 CI 与合入

## 1. 可证伪假设

基线存在三类冲突：

1. `CONTRIBUTING.md` 要求从 `master` 创建普通分支并把 PR 合回 `master`；
2. 发布计划要求 `develop` 是唯一集成和默认分支，`master` 仅保存 Release Gate 通过的基线；
3. 仓库没有执行级 PR 模板，实际默认分支和 Ruleset 尚未对齐。

核对后假设成立。

## 2. Part A：仓库文件治理

PR #8 已合入 `develop`：

- `CONTRIBUTING.md`：普通工作从 `develop` 创建并以 `develop` 为 PR 目标；`master` 只接收 G3 通过后的授权 RC PR。
- `.github/pull_request_template.md`：要求 Work ID、依赖、Requirement/Test/CR/XF、假设、三层证据、测试计数、数据/隐私/安全、兼容/迁移、风险/回滚和明确不做事项。
- `docs/contributing/BRANCH_POLICY.md`：定义 Branch Role、正常集成流、Release Flow、Ruleset、紧急恢复和验证清单。
- CI Run #42 / `30289745122`：Python 3.10、3.11、3.12 全部 Success。

本部分未修改业务代码、package metadata、Plugin manifest、installer、workflow 或 Runtime 行为。

## 3. Part B：GitHub 设置验证

### 3.1 默认分支

REL-002 开始时：

```text
default_branch = master
```

用户完成设置后，GitHub 连接器重新查询仓库元数据，机器确认：

```text
default_branch = develop
admin_permission = true
```

### 3.2 Ruleset

仓库管理员在 2026-07-28 明确确认已完成：

```text
develop: require PR + require current CI + prohibit force push + prohibit deletion
master:  require PR + require current CI + prohibit force push + prohibit deletion
```

验证边界：当前 GitHub 连接器没有 Ruleset/Branch Protection 枚举接口，本地环境也没有可认证的 `gh` CLI。因此默认分支是机器证据，Ruleset 是管理员人工验证证据。本文不声称连接器读取到了 Ruleset JSON。

`QA-002` 后续仍须把 Ruff、format、type、ShellCheck、coverage、dependency 和 secret checks 加入机器可验证的 Required Checks。

## 4. Part C：主计划无损合并

采用无损 Consolidation：

- 原 `OPEN_SOURCE_RELEASE_PLAN.md` v2.2 全文以相同 Blob 保存到 `docs/roadmap/archive/OPEN_SOURCE_RELEASE_PLAN_2.2.md`；
- 新 `OPEN_SOURCE_RELEASE_PLAN.md` 为规范性 v2.3 主计划；
- 未修改的 v2.2 任务账本、48 Work ID、文件所有权、测试入口、Gate 和附录按引用继续生效；
- 临时 `OPEN_SOURCE_RELEASE_PLAN_ERRATA_2.3.md` 已删除；
- 四项修正已进入 v2.3 主计划：
  - RC/正式 Tag 与 `test-release` 顺序；
  - `REL-004` 只冻结 10 Tool 目标；
  - `XF-DEPS-001` / `XF-RELEASE-001` 规范 ID；
  - `BETA-003` 依赖和完成条件。

该方案避免了整文件手工复制导致 1100+ 行任务账本丢失或漂移的风险。

## 5. 验证清单

- [x] 普通分支从 `develop` 创建。
- [x] 普通 PR 目标是 `develop`。
- [x] `master` 只接收授权 Release PR。
- [x] PR 模板包含执行与回滚证据字段。
- [x] 测试隔离要求临时 HOME/DB/port/Fake Secret。
- [x] 活动贡献指南中“从 master 创建普通分支/普通 PR 到 master”冲突搜索为 0。
- [x] PR #8 Python 3.10–3.12 CI 全绿并合入。
- [x] 默认分支机器确认是 `develop`。
- [x] `develop` Ruleset 由仓库管理员确认。
- [x] `master` Ruleset 由仓库管理员确认。
- [x] v2.2 全文无损归档。
- [x] Errata 四项修正并入 v2.3 主计划并删除临时文件。
- [ ] Plan Consolidation PR Python 3.10–3.12 CI 全绿并合入。

## 6. Gate 结论

当前 `REL-002` 为 `IN_REVIEW`。Plan Consolidation PR 合入后可关闭 Issue #7，并允许开始串行后继 `REL-003`。

`REL-002` 完成不等于 G0 通过；G0 仍需 `REL-003`、`REL-004` 及其余 M0 必需项。
