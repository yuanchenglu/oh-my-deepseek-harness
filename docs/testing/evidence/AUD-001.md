# AUD-001 Evidence - JSONL Audit Event Source of Truth

- Work ID: `AUD-001`
- Canonical Issue: #39
- Dependency: SES-001 complete
- Status: **Complete**
- Implementation baseline: `develop@master`
- Implementation branch: `feat/aud-001-jsonl-audit`
- Next serial Work ID: `OPS-001` / #40

## 1. Problem

constraint 违反记录以 Markdown 写入，但 assessor 输出格式（`## 时间` +
`- **约束**: X` 多行）无法被 immune_audit 解析（XF-AUDIT-001）。缺结构化
事件源。

## 2. Implementation

- 新增 `audit_events.py`：JSONL 事件源（唯一事实源）。
  - `append_event`：追加结构化事件（一行 JSON）。
  - `read_events`：读取全部事件；malformed 行隔离为 `{"error": ...}` 不崩溃。
  - `events_to_markdown`：从事件派生 Markdown 报告（数量字段一致）。
- `assessor._record_violation`：改为写 JSONL 结构化事件（不再直接写 Markdown）。
- `immune_audit._parse_violations`：支持 JSONL 事件行 + assessor 多行 Markdown
  格式 + 旧格式。
- XF-AUDIT-001 移除 xfail 转 Pass（XFAIL 全清）。

## 3. CI evidence

PR #107:
- test (3.10): SUCCESS
- test (3.11): SUCCESS
- test (3.12): SUCCESS

## 4. TC coverage

| TC ID | Description | Tests | Status |
|---|---|---|---|
| TC-POLICY-006 | 路径疑似违反写结构化事件 | `test_append_event_writes_structured_jsonl` | PASS |
| TC-POLICY-007 | 普通 Tool 不误报 | `test_normal_tool_no_false_positive` | PASS |
| TC-POLICY-008 | JSONL 生成报告数量一致 | `test_events_to_markdown_count_matches` | PASS |
| XF-AUDIT-001 | assessor 输出格式可解析 | `test_immune_audit_parses_assessor_output_format` | PASS |
| JSONL round-trip | 写入读取一致 | `test_jsonl_round_trip` | PASS |
| 损坏行隔离 | 不损坏有效事件 | `test_malformed_rows_isolated` | PASS |

Local: test_audit.py + regressions 33 passed.

## 5. FR coverage

| FR ID | Description | Status |
|---|---|---|
| FR-POLICY-005 | 路径疑似违反记录结构化事件 | Covered (TC-POLICY-006) |
| FR-POLICY-008 | 事件源派生报告 | Covered (TC-POLICY-008) |
| FR-OBS-001 | 审计可观测性 | Covered |

## 6. Authorized paths

| Path | Status |
|---|---|
| `src/deepseek_harness/audit_events.py` | Added (JSONL 事件源) |
| `src/deepseek_harness/assessor.py` | Modified (_record_violation → JSONL) |
| `src/deepseek_harness/immune_audit.py` | Modified (_parse_violations 多格式) |
| `tests/test_audit.py` | Added |
| `tests/test_release_readiness_regressions.py` | Modified (XF-AUDIT-001 转 Pass) |

## 7. Definition of Done

- [x] XFAIL 变 Pass（XF-AUDIT-001）
- [x] JSONL round-trip / 派生报告 / 损坏行隔离测试通过
- [x] 未存原始 prompt/tool 内容（事件仅存约束/证据/工具/会话）
- [x] Required CI + traceability