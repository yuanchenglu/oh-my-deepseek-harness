# OPS-001 Evidence - Manual Audit CLI & No Scheduler Side Effects

- Work ID: `OPS-001`
- Canonical Issue: #40
- Dependency: AUD-001 complete
- Status: **Complete**
- Implementation baseline: `develop@master`
- Implementation branch: `feat/ops-001-audit-cli`
- Next serial Work ID: `INTENT-001` / #41

## 1. Problem

审计无统一入口；cron 示例可能被当作安装机制，担心 install/uninstall 改
scheduler。

## 2. Implementation

- `cli audit` 子命令：从 JSONL 事件源读取，派生 Markdown 报告；`--json`
  输出固定结构（state/event_count/valid_count/report）。
- `crons/immune-audit.cron` 保持非安装示例（installer 不安装 cron，无
  crontab/systemd/launchd 引用）。
- 测试验证 scheduler 快照在 audit 前后不变（TC-AUDIT-CLI-001）。

## 3. CI evidence

PR #108:
- test (3.10): SUCCESS
- test (3.11): SUCCESS
- test (3.12): SUCCESS

## 4. TC coverage

| TC ID | Description | Tests | Status |
|---|---|---|---|
| TC-AUDIT-CLI-001 | 手工 audit 且不修改 scheduler | `test_audit_cli_generates_report` | PASS |
| OPS-001 | audit --json 固定契约 | `test_audit_cli_json_contract` | PASS |

Local: test_audit.py 7 passed.

## 5. FR coverage

| FR ID | Description | Status |
|---|---|---|
| FR-POLICY-008 | 手工 audit 报告 | Covered (TC-AUDIT-CLI-001) |
| FR-OBS-005 | 运行时可观测性 | Covered |

## 6. Authorized paths

| Path | Status |
|---|---|
| `src/deepseek_harness/cli.py` | Modified (audit 子命令) |
| `src/deepseek_harness/immune_audit.py` | Unchanged |
| `crons/immune-audit.cron` | Unchanged (非安装示例) |
| `tests/test_audit.py` | Modified (TC-AUDIT-CLI-001 + json 契约) |

## 7. Definition of Done

- [x] 手工 audit 正常工作，JSON 输出符合固定契约
- [x] scheduler 快照前后不变（crontab 无修改）
- [x] 示例无用户特定绝对路径
- [x] Required CI + traceability