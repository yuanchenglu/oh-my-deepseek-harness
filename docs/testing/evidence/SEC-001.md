# SEC-001 Evidence - Dependency / License / SBOM Review

- Work ID: `SEC-001`
- Canonical Issue: #47
- Dependency: COMPAT-001 + PRIV-001 complete
- Status: **Complete**
- Implementation baseline: `develop@master`
- Implementation branch: `feat/sec-001`
- Next serial Work ID: `MIG-001` / #48

## 1. Problem

第三方依赖许可证不可追溯、无 SBOM、依赖漏洞无扫描、发布权限未最小化。

## 2. Implementation

- `scripts/security/sbom.sh`：从 pyproject.toml 生成 CycloneDX-light JSON SBOM
  （base + all extras，Python 3.10 tomli fallback）。
- `scripts/security/audit_deps.sh`：FR-SEC-005 依赖漏洞扫描（pip-audit 优先，
  无则 fallback 依赖清单 inventory）。
- `docs/release/RELEASE_CHECKLIST.md`：发布安全清单（依赖/SBOM/权限最小化/
  artifact 身份/安全门/evidence/rollback）。未豁免 High/Critical 阻断发布。
- SECURITY.md 已存在且完整（含 RELEASE_CHECKLIST 引用）。

## 3. CI evidence

PR #120:
- test (3.10): SUCCESS
- test (3.11): SUCCESS
- test (3.12): SUCCESS
- qa-fast: SUCCESS
- qa-quality: SUCCESS

## 4. FR coverage

| FR ID | Description | Status |
|---|---|---|
| FR-SEC-005 | 依赖漏洞扫描，未豁免 High/Critical 阻断 | Covered (audit_deps.sh) |
| FR-QA-008 | Known Defects strict XFAIL | Covered |
| FR-QA-009 | Release Gate | Covered (RELEASE_CHECKLIST) |

## 5. Verification

Local: test_sec.py 4 passed; 核心回归 131 passed; sbom.sh 生成 14 组件 SBOM.

## 6. Authorized paths

| Path | Status |
|---|---|
| `scripts/security/sbom.sh` | Added |
| `scripts/security/audit_deps.sh` | Added |
| `docs/release/RELEASE_CHECKLIST.md` | Added |
| `tests/test_sec.py` | Added (授权例外) |

## 7. Definition of Done

- [x] 第三方许可证可追溯（SBOM 组件清单）
- [x] SBOM 与 wheel 身份匹配（checklist 校验）
- [x] 依赖漏洞扫描（FR-SEC-005）
- [x] 发布权限最小化（checklist 记录 OIDC/Trusted Publisher）
- [x] 未豁免 High/Critical 阻断发布
- [x] Required CI + traceability

## 8. Residual (honest)

发布权限最小化（OIDC/Trusted Publisher 实际配置）由 REL-006 在 release.yml
落实；本 Work ID 记录 checklist 要求。pip-audit CVE 扫描需 CI 安装，本地
fallback 为依赖清单 inventory。