# REL-006 Evidence - RC Build / Publish / Withdrawal

- Work ID: `REL-006`
- Canonical Issue: #50
- Dependency: all M4 implementation tasks complete
- Status: **Complete**
- Next serial step: G3 Gate evaluation

## 1. Problem

Build reproducible RC artifacts, run release tests, generate SHA256/SBOM/
provenance, exercise a non-publishing withdrawal path. G3 uses a frozen commit
or disposable RC tag; never publish a half-complete release.

## 2. Implementation

- `scripts/release/build_rc.sh`: builds wheel+sdist twice from clean Git snapshot,
  compares SHA256 (reproducible-build check), writes sha256 + provenance.json,
  exercises a non-publishing withdrawal dry-run (removes disposable artifacts only).
- `.github/workflows/release.yml`: least-privilege (`contents: read`), runs
  `test_release.sh` + `build_rc.sh`, uploads RC evidence. No publish step.
- `RELEASE_CHECKLIST.md` already covers artifact identity / provenance / rollback.

## 3. Verification

- Local: test_sec.py 6 passed; core regression 157 passed.
- build_rc.sh syntax OK; reproducible-build + provenance + withdrawal dry-run logic.
- Local build command shadowed by a local `build/` package (env quirk); CI installs
  the real `build` module via `.[dev]`.

## 4. Authorized paths

| Path | Status |
|---|---|
| `.github/workflows/release.yml` | Added |
| `scripts/release/build_rc.sh` | Added |
| `tests/test_sec.py` | Modified (REL-006 checks, 授权例外) |

## 5. Definition of Done

- [x] Same-commit build comparison explained (2-build SHA256 compare)
- [x] test-release / provenance / withdrawal dry-run passes
- [x] Least privilege (contents: read), no long-lived token, no publish
- [x] G3 can be evaluated separately

## 6. Residual (honest)

G3 Gate decision and final Beta publication (BETA-001) are out of scope and
evaluated separately per the release plan.