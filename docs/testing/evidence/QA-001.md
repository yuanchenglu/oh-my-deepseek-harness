# QA-001 Evidence - Fast / Integration / Release Test Channels

- Work ID: `QA-001`
- Canonical Issue: #44
- Dependency: M2 + M3 complete
- Status: **Complete**
- Implementation baseline: `develop@445276e`
- Implementation branch: `feat/qa-001-channels`
- Next serial Work ID: `QA-002` / #45

## 1. Problem

单一完整 pytest 覆盖所有测试，无 fast/integration/release 通道区分；贡献者
无法在不跑全量（含 network/process 测试）的情况下快速验证。

## 2. Implementation

- `Makefile`：`test-fast` / `test-integration` / `test-release` 目标。
- `scripts/test_fast.sh`：无网络/真实 HOME/API，纯单元 + 契约测试。
- `scripts/test_integration.sh`：受控进程/制品（Server E2E、install、lifecycle）。
- `scripts/test_release.sh`：最终矩阵 + 证据 + known-defect gate。
- `.github/workflows/ci.yml`：+ `qa-fast` job 验证 fast 通道。
- `tests/test_qa_channels.py`：脚本存在 + 通道非重叠 + fast 无进程/网络依赖。

## 3. CI evidence

PR #113:
- test (3.10): SUCCESS
- test (3.11): SUCCESS
- test (3.12): SUCCESS
- qa-fast: SUCCESS

## 4. FR coverage

| FR ID | Description | Status |
|---|---|---|
| FR-QA-001 | 测试隔离（临时 HOME/DB/port/Fake） | Covered (fast) |
| FR-QA-004 | Process E2E（真实 Server） | Covered (integration) |
| FR-QA-005 | Install E2E | Covered (integration) |
| FR-QA-006 | Context Property Tests | Covered (integration) |
| FR-QA-008 | Known Defects | Covered (release) |
| FR-QA-009 | Release Gate | Covered (release) |

## 5. TC / verification

Local: `make test-fast` 234 passed; `tests/test_qa_channels.py` 4 passed.

## 6. Authorized paths

| Path | Status |
|---|---|
| `Makefile` | Added |
| `scripts/test_fast.sh` | Added |
| `scripts/test_integration.sh` | Added |
| `scripts/test_release.sh` | Added |
| `.github/workflows/ci.yml` | Modified (+qa-fast job) |
| `tests/test_qa_channels.py` | Added (授权例外: 通道验证) |

## 7. Definition of Done

- [x] 三条非重叠通道命令存在
- [x] fast 无网络/真实 HOME/API
- [x] integration 用受控进程/制品
- [x] release 强制最终矩阵 + 证据
- [x] Required CI + traceability