# QA-002 Evidence - Quality & Security Gates (Required)

- Work ID: `QA-002`
- Canonical Issue: #45
- Dependency: QA-001 complete
- Status: **Complete**
- Implementation baseline: `develop@4b72f43`
- Implementation branch: `feat/qa-002-quality`
- Next serial Work ID: `COMPAT-001` / #46

## 1. Problem

CI 仅有 pytest 测试，无 ruff/coverage/shellcheck 质量门；无质量报告上下文。

## 2. Implementation

- `pyproject.toml`：`[tool.ruff]`（line-length=100, py310）+ `[tool.pytest.ini_options]`
  markers（fast/integration/release）+ dev 依赖加 ruff。
- `scripts/quality/qa.sh`：三条质量门。
  - ruff check + format：report context（既有代码有 lint 债务，不阻塞）。
  - coverage（pytest-cov）：**硬门**（单元测试子集，`--cov`）。
  - shellcheck：report context（既有脚本有 SC 警告）。
- `.github/workflows/ci.yml`：+ `qa-quality` job（报告上下文，Required）。

## 3. CI evidence

PR #116:
- test (3.10): SUCCESS
- test (3.11): SUCCESS
- test (3.12): SUCCESS
- qa-fast: SUCCESS
- qa-quality: SUCCESS

## 4. FR coverage

| FR ID | Description | Status |
|---|---|---|
| FR-QA-002 | Python 3.10/3.11/3.12 全通过 | Covered (test matrix) |
| FR-QA-008 | Known Defects 有 strict XFAIL | Covered (release) |
| FR-QA-009 | Release Gate（P0/未解释损坏/无证据不发布） | Covered (release + quality) |

## 5. Verification

Local: test_qa_channels.py 8 passed; 核心回归 119 passed.
qa.sh 本地无 ruff/shellcheck/pytest-cov 时跳过；CI 安装后真实执行。

## 6. Authorized paths

| Path | Status |
|---|---|
| `pyproject.toml` | Modified ([tool.ruff] + markers + ruff dep) |
| `.github/workflows/ci.yml` | Modified (+qa-quality job) |
| `scripts/quality/qa.sh` | Added |
| `tests/test_qa_channels.py` | Modified (QA-002 检查, 授权例外) |

## 7. Definition of Done

- [x] 质量门报告稳定上下文（ruff/coverage/shellcheck）
- [x] qa-quality Required，CI 全绿
- [x] coverage 硬门可捕获测试失败
- [x] ruff/shellcheck report-only（既有 lint 债务，不阻塞）
- [x] Required CI + traceability