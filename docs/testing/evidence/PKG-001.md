# PKG-001 Evidence：标准可安装包布局

- Work ID：`PKG-001`
- Issue：#18
- Pull Request：#62
- 分支：`feat/pkg-001-standard-layout`
- 状态：Ready to merge；Required CI 已通过，待最终 Head 复核与 Squash Merge
- G0 基线：`develop@ee516c9b201f3b995781775a65d81da0ce21ecae`
- 执行日期：2026-07-28

## 1. 目标

生产 Python 实现已收敛到唯一 `src/` 布局：

```text
src/deepseek_harness/
src/deepseek_context/
src/harness_server/
```

旧 `plugins/**` 与 `mcp/harness_server/**` 不保留第二份业务实现，只保留 Hermes/source-checkout 兼容转发。

## 2. 包契约

- Python distribution：`oh-my-deepseek-harness==3.0.0b1`；
- package discovery：仅 `src/`；
- Hermes pip entry-point group：`hermes_agent.plugins`；
- entry points：
  - `deepseek-harness = deepseek_harness`；
  - `deepseek-context = deepseek_context.plugin`；
- package data：
  - Harness `plugin.yaml`、`strategies.yaml`；
  - Context `plugin.yaml`、`config.yaml`；
  - Server `config.yaml`。

Hermes v0.19.0 对 entry point 执行 `ep.load()`，取得模块后调用模块的 `register(ctx)`。本布局与该正式契约一致。

## 3. 迁移结果

保持不变：

- 10 个目标 Tool / 9 个当前 Runtime Tool；
- Hook、Schema、API、DB 和领域逻辑；
- Plugin Manifest 版本；
- Python 3.10–3.12 package/core CI；
- Hermes v0.19.0 完整集成仅 Python 3.11–3.12。

必要路径修正：

- Server 启动由源码脚本相对路径改为 `python -m harness_server.server`；
- Intent strategies 使用 `importlib.resources` 从 wheel 资源读取；
- `tests/conftest.py` 删除 `sys.path` 注入与下划线 symlink；
- 5 个现有测试文件删除旧 `mcp/`/`plugins/` path injection 或改读 canonical `src/`；
- COMPAT/G0 静态探针改读 `src/` canonical source。

## 4. 三层验证

### 4.1 文件层

- wheel 只包含 `deepseek_harness`、`deepseek_context`、`harness_server`；
- Harness/Context/Server YAML/config resources 均存在；
- wheel 不包含 `plugins/` 或 `mcp/` 生产副本；
- 旧目录只包含 manifest/register 或 module forwarding。

### 4.2 集成层

`tests/test_package_artifact.py` 从 `git archive HEAD`：

1. 构建 wheel；
2. 创建带 system-site-packages 的临时 venv；
3. `pip install --no-deps <wheel>`；
4. 在仓库外导入三个公共 package；
5. 断言模块文件不来自 repository；
6. 用 `importlib.metadata` 加载两个 Hermes entry points；
7. 用 `importlib.resources` 读取全部资源；
8. 确认 Tool Server 目标为 module，且不存在 `_SERVER_SCRIPT`。

### 4.3 Pipeline 层

CI Run：`30324673432`（Run #81）

```text
Python 3.10: success — 176 tests, 0 failed, 0 errors, 11 strict XFAIL
Python 3.11: success — complete suite + artifact lifecycle
Python 3.12: success — complete suite + artifact lifecycle
```

Python 3.10 JUnit artifact：`pytest-3.10` / artifact `8675202559` / SHA256 digest `8f67f1db3edbe8fee19cb69902cdc49cd208acdc998542f63811f91b15f724a5`。

11 个 XFAIL 均为进入 PKG-001 前已存在、且由后续 Work ID 管理的缺陷；本任务未新增 XFAIL、未把失败改成 XFAIL。

## 5. 失败与修正记录

### Run #77

收集失败：三个测试显式将旧源码路径插入 `sys.path`。修正为已安装 package import，没有恢复 symlink/path hack。

### Run #80

两个安全静态回归仍读取旧 Server 转发文件。修正为读取 `src/harness_server/server.py` 和 `storage.py`；loopback 与字段白名单断言保持原样。

### Run #81

三版本全绿，制品生命周期通过。

## 6. 未越界事项

本 Work ID 未实现：

- Dependency extras 重构（`PKG-002`）；
- CLI、Installer、Doctor、Upgrade/Uninstall（`RUN/INS`）；
- Server Supervisor；
- Tool/API/Storage schema 变更；
- `memory_store`；
- Hermes 真实 OS/Python E2E（`COMPAT-001`）；
- Tag、Release 或 PyPI 发布。

## 7. Merge-time fields

```text
PR: #62
Squash commit: recorded in Issue #18 completion comment after protected merge
Final CI run: pending final evidence-only Head
Python 3.10: must remain success
Python 3.11: must remain success
Python 3.12: must remain success
Wheel inventory: PASS on Run #81
External import: PASS on Run #81
```
