# MIG-001 Evidence - Beta Migration and Failure Rollback

- Work ID: `MIG-001`
- Canonical Issue: #48
- Dependency: INS-003 + MEM-002 + AUD-001 complete
- Status: **Complete**
- Next serial Work ID: `SEC-002` / #49

## 1. Problem

Config/DB/constraint events/process state migration from review baseline to Beta,
with per-stage failure rollback and downgrade rejection.

## 2. Implementation

- `migration.py::migrate_constraint_log()`: reads old `constraint-violations.md`,
  converts valid lines to JSONL events via `audit_events.append_event`; invalid
  lines preserved in original file (TC-MIG-003).
- `migration.py::reject_downgrade()`: version comparison before writes; raises
  ValueError if target < current (TC-MIG-006).
- `migration.py::migration_inventory()`: before/after snapshot of Config/DB/events
  (TC-MIG-001).
- `lifecycle.py::upgrade()` already handles backup/process-stop/restart/rollback
  with phase_hook for failure injection (TC-MIG-004/005).

## 3. TC coverage

| TC ID | Description | Status |
|---|---|---|
| TC-MIG-001 | Migration inventory (Config/DB/events) | PASS |
| TC-MIG-002 | dry-run no-write, repeat not duplicated | PASS |
| TC-MIG-003 | constraint-violations.md -> JSONL; invalid preserved | PASS |
| TC-MIG-004 | process stop on upgrade | PASS (lifecycle) |
| TC-MIG-005 | failure injection rollback | PASS |
| TC-MIG-006 | downgrade rejection before writes | PASS |

## 4. Verification

Local: test_migration.py 13 passed; core regression 144 passed.

## 5. Authorized paths

| Path | Status |
|---|---|
| `src/deepseek_harness/migration.py` | Added |
| `tests/test_migration.py` | Added |
