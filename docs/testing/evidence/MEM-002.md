# MEM-002 Evidence - Safe Memory Import/Delete

- Work ID: `MEM-002`
- Canonical Issue: #34
- Dependency: MEM-001 complete
- Status: **Complete**
- Implementation baseline: `develop@d844b5c`
- Implementation branch: `feat/mem-002-memory-import-delete`
- Next serial Work ID: `PLAN-001` / #35

## 1. Problem

记忆导入/删除不安全：缺按 source 删除能力、缺 dry-run 预览、CLI 删除无确认保护。
startup import 需默认关闭避免误扫用户 Memory。

## 2. Implementation

- `storage.delete_memories_by_source(source)`: 按 source 删除，source 边界断言（TC-MEM-009）。
- `cli memory import [--dry-run]`: 导入 Hermes Memory .md；--dry-run 用临时 store 只统计不写库（TC-MEM-012）。
- `cli memory delete --source X [--confirm]`: 缺 --confirm 返回 4 拒绝（FR-MEMORY-006）。
- startup 默认关闭：`RuntimeConfig.import_memories=False`（TC-MEM-011），已满足。
- 文件变更幂等：MEM-001 的 content_hash 保证重复导入不增长（TC-MEM-005/006）。

## 3. CI evidence

PR #102:
- test (3.10): SUCCESS
- test (3.11): SUCCESS
- test (3.12): SUCCESS

## 4. TC coverage

| TC ID | Description | Tests | Status |
|---|---|---|---|
| TC-MEM-005 | Import 重启三次 → 条目不增 | `test_import_restart_three_times_no_growth` | PASS |
| TC-MEM-006 | 文件变更 → 更新/新版本不重复旧数据 | `test_import_file_change_does_not_duplicate_old` | PASS |
| TC-MEM-009 | Delete by source → 仅删目标 | `test_delete_by_source_only_removes_target` | PASS |
| TC-MEM-011 | 默认配置启动三次 → 不扫描用户 Memory | `test_default_config_does_not_import_on_startup` | PASS |
| TC-MEM-012 | Import dry-run → 逐文件报告 + DB 不变 | `test_import_dry_run_does_not_write_db` | PASS |
| TC-MEM-013 | 损坏 UTF-8/截断/取消 → 可诊断无半批次 | `test_import_corrupt_utf8_is_diagnosable` | PASS |
| FR-MEMORY-006 | Delete 缺 --confirm 拒绝 | `test_delete_requires_confirm` | PASS |

Local: test_memory.py 15 passed.

## 5. FR coverage

| FR ID | Description | Status |
|---|---|---|
| FR-MEMORY-005 | Import dry-run/幂等/变更检测/截断提示/取消 | Covered (TC-MEM-005/006/012/013) |
| FR-MEMORY-006 | Delete 按 ID/Source，CLI 缺 --confirm 拒绝 | Covered (TC-MEM-009, confirm) |

## 6. Authorized paths

| Path | Status |
|---|---|
| `src/harness_server/storage.py` | Modified (delete_memories_by_source) |
| `src/deepseek_harness/cli.py` | Modified (memory import/delete) |
| `tests/test_memory.py` | Modified (TC-MEM-005/006/009/011/012/013) |
| `src/harness_server/models.py` | Unchanged |
| `src/harness_server/server.py` | Unchanged |
| `tests/fixtures/memory/**` | Not needed |

## 7. Definition of Done

- [x] XFAIL(XF-MEM-001) 已在 MEM-001 转 Pass
- [x] startup/dry-run/delete/corruption 测试通过（15/15）
- [x] 无半批次（import 用临时 store 或幂等写入）
- [x] 未触碰真实 Memory/HOME/Secret；autouse HOME 隔离
- [x] 未增加公共 Tool（tool 注册 ∈ MEM-002 范围外，保持 9→10 目标由 MEM-001 的 memory_store 达成）