# RUN-001 Evidence：App Factory 与 Runtime 探针

- Work ID：`RUN-001`
- Issue：#20
- Pull Request：#64
- 分支：`feat/run-001-app-factory`
- 状态：Ready to merge；首轮 Required CI 已通过，待最终 Evidence Head 复核与 Squash Merge
- 依赖基线：`develop@ae277d1d860713ec6bca7dd1b1eabd6632cd142e`
- 执行日期：2026-07-28

## 1. Runtime 结构

公开入口：

- `harness_server.server.create_app()`：可注入 `RuntimeConfig`、Storage 或 Storage Factory；
- `python -m harness_server.server`：通过 Uvicorn factory mode 启动；
- `harness_server.config.RuntimeConfig`：唯一环境变量契约。

原有 Plan/Memory/Checkpoint 业务路由原样保存在内部 `harness_server.app`。公开 Factory 只复制 `/plan/**`、`/memory/**`、`/checkpoint/**`，不复制旧 `/health`。

## 2. 单一环境变量契约

```text
HARNESS_HOST
HARNESS_PORT
HARNESS_DB_PATH
HARNESS_IMPORT_MEMORIES
HARNESS_MEMORIES_DIR
```

规则：

- 默认 Host：`127.0.0.1`；
- 仅接受 IPv4/IPv6 loopback 或 `localhost`；
- Port 必须为 1–65535；
- Memory import 默认关闭；
- DB/Memory 路径在 `RuntimeConfig` 创建时展开；
- 旧 Server Port/URL 环境变量在 active source/tests 中为 0 references，并被配置解析器忽略。

远程监听、认证与 TLS 不属于 RUN-001。

## 3. Import 副作用边界

`import harness_server.server`：

- 不创建 SQLite；
- 不创建 `~/.hermes`；
- 不扫描 Hermes Memory；
- 不启动进程；
- 不加载内部业务路由模块。

Storage 创建发生在 `create_app()`；可选 Memory import 只发生在 FastAPI lifespan，且需要 `HARNESS_IMPORT_MEMORIES=true`。

内部历史路由首次加载时：

- Storage constructor 被 Factory 注入；
- HOME/USERPROFILE 暂时指向空临时目录；
- 不读取真实用户 Memory；
- 加载后所有 endpoint globals 指向 Factory 的 Storage。

## 4. 探针语义

| Endpoint | 含义 | DB 依赖 | 响应安全 |
|---|---|---|---|
| `/health` | 进程 liveness | 无 | 仅 `status/service` |
| `/ready` | SQLite readiness | 有 | 成功 `ready`；失败固定 `storage_unavailable` |
| `/version` | Runtime/API 版本 | 无 | distribution version + api_version |

失败注入结果：Storage 构造对象可用、连接失败时，`/health` 与 `/version` 保持 200；`/ready` 返回 503；三个响应均不包含 DB path、HOME、Memory path、异常文本或环境值。

## 5. 三层验证

### 5.1 文件层

- 公开 `server.py` 只包含 Factory、探针和启动入口；
- 原领域路由完整迁入内部 `app.py`；
- 新增唯一 `config.py` 环境契约；
- Tool Client 从 `RuntimeConfig.server_url` 解析地址；
- 未修改 `models.py`、`storage.py`、Workflow 或领域 Schema。

### 5.2 集成层

`tests/test_harness_server.py`：

- 注入临时 DB 的 App Factory；
- 三个探针；
- 原有 Plan/Memory/Checkpoint 核心端点；
- 默认启动不导入 Memory。

`tests/test_server_process.py`：

1. 隔离 subprocess import，断言无 DB/`~/.hermes`；
2. 环境变量有效/无效矩阵；
3. legacy env 名称被忽略且 active source/tests 无引用；
4. BrokenStorage 注入：health/version 仍成功、ready 503 且不泄露异常；
5. 真实子进程启动，轮询 health→ready→version，验证临时 DB 创建并安全终止。

PKG-001 制品生命周期与 PKG-002 base/context/server/all 四种 clean-install 矩阵在同一完整套件中继续通过。

### 5.3 Pipeline 层

首轮 CI：Run #87 / ID `30326673572`

```text
Python 3.10: success — 189 tests, 0 failures, 0 errors, 10 pre-existing strict XFAIL
Python 3.11: success — complete suite + wheel/dependency/process lifecycle
Python 3.12: success — complete suite + wheel/dependency/process lifecycle
```

Python 3.10 JUnit artifact：

```text
artifact: pytest-3.10
artifact id: 8675923047
digest: sha256:f3ab8b0c4a8e8172e6d5fe3bdcad1945eb4db9fb618fac2cb67ea8e30c493bc3
suite: 189 tests / 0 failures / 0 errors / 10 skipped(strict XFAIL)
time: 50.504s
```

RUN-001 新增 8 个普通通过测试；strict XFAIL 数量保持 10，本任务未新增或改写 XFAIL。

## 6. 关键验收结果

```text
module import side effect: PASS
SQLite created during import: NO
Hermes Memory scanned during import: NO
loopback default/rejection: PASS
health liveness independent of DB: PASS
ready storage check/redacted failure: PASS
version protocol: PASS
real process health→ready→version: PASS
legacy active env references: 0
existing domain endpoint regressions: PASS
```

## 7. 范围

本 Work ID 未实现：

- PID/lock/log/status/stop Supervisor（RUN-002）；
- Installer/Doctor；
- Remote binding/auth/TLS；
- Tool/API/DB schema 或领域行为；
- Hermes E2E；
- Tag、Release、PyPI。

## 8. Merge-time fields

```text
PR: #64
Squash commit: recorded in Issue #20 after protected merge
Final CI run: pending final Evidence-only Head
Python 3.10: must remain success
Python 3.11: must remain success
Python 3.12: must remain success
import side effect: PASS on Run #87
health/ready/version: PASS on Run #87
real process readiness: PASS on Run #87
legacy env references: 0 on Run #87
```
